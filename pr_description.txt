## Agent Handoff

Status: Ready for review
Owner: Jules
Inputs: Request to build an export script mapping the database to Quod Libet compatible CSVs based on attached schemadocs.
Outputs: `scripts/export_ql_csv.py`
Counts: 1 Python script created, successfully exporting 9672 test records to CSV. Tests run successfully.
Safety: Verified logic avoids unsafe SQL ops, reads cleanly using read-only mode, and correctly handles both the documented unified view (`v_unified_song_performances`) and the local fallback `recordings` table structure found in `music.sqlite`. File defaults to write in `data/exports`.
