Add PyInstaller packaging for desktop application

This update fixes the PyInstaller fork bomb issue by converting the
BackendManager from `subprocess` to `multiprocessing` with `freeze_support()`.
It also updates the FastAPI backend to correctly serve the static
frontend files from the PyInstaller `_MEIPASS` bundle, and adds
a `package_desktop.sh` build script.

Status: review_passed
Owner: Jules
Inputs:
- apps/musicdb_desktop/backend_manager.py
- apps/musicdb_desktop/main.py
- api/main.py
Outputs:
- package_desktop.sh
- Executable in dist/MusicDB
Counts: 3 updated files, 1 build script added.
Safety: 100% (No database writes. Fork bomb prevented via freeze_support).
Validation: pytest tests/test_desktop_wrapper.py (All passed) and PyInstaller test build.
Blocked/Questions: None.
Next: Ready for desktop use or distribution.
