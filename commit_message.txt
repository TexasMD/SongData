fix(split_billboard_artists): handle pandas NaN values gracefully

Resolves a `TypeError: expected string or bytes-like object, got 'float'` crash when pandas parses empty artist name cells in the input CSV as `np.nan` floats.
